[package]
name = "tailrocks-repository-helper"
version = "0.1.0"
edition = "2021"
description = "Deterministic mechanics for target-aware Tailrocks repository campaigns"
license = "Apache-2.0"

[dependencies]
fs2 = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3.13"

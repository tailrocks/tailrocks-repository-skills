interface:
  display_name: "Tailrocks: Repository Cleanup"
  short_description: "Remove only proven resolved repository sources"
  default_prompt: "Use $tailrocks-repository-skills:tailrocks-repository-cleanup for an explicit scoped cleanup request. Prove target resolution, obligations, ownership, quiescence, and restore safety first."
policy:
  allow_implicit_invocation: false
